# CLI Interface

## ADDED Requirements

### Requirement: CLI-001
The system SHALL provide a CLI interface.

#### Scenario: CLI Available
Given the library is installed,
When user runs `linkbio --help`,
Then CLI interface is available.